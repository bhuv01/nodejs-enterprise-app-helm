apiVersion: v1
kind: Service
metadata:
  name: {{ include "server-info.fullname" . }}
  labels:
    {{- include "server-info.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "server-info.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
